import ComputersCanvas from "./ComputersCanvas";
import Developer from "./Developer";
import StarsCanvas from "./StarsCanvas";

/**
 * Exports the following components:
 * - ComputersCanvas: A component that renders a 3D canvas with computer models.
 * - Developer: A component that represents a developer in the 3D canvas.
 * - StarsCanvas: A component that renders a 3D canvas with star models.
 */
export { ComputersCanvas, Developer, StarsCanvas };
